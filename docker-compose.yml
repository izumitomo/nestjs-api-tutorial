version: '3.8'
services:
  dev-db:
    image: postgres:14
    ports:
      - 3457:3458
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: nest
    networks:
      - freecodecamp
networks:
  freecodecamp:
